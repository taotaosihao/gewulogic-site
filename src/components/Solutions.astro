---
import { solutions } from "../data/site";
---

<section id="solutions" class="py-24 bg-white">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="mx-auto max-w-3xl text-center mb-16">
      <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">解决方案</h2>
      <p class="mt-4 text-lg text-gray-600">
        围绕制造企业的"数据底座 → 执行主线 → 运营协同"三层架构，构建完整的工业数字化能力
      </p>
    </div>

    <div class="grid gap-8 lg:grid-cols-3">
      {solutions.map((s) => (
        <div class="group relative rounded-2xl bg-white p-8 shadow-sm ring-1 ring-gray-200 transition-all hover:shadow-lg hover:-translate-y-1">
          <div class="mb-6 inline-flex h-12 w-12 items-center justify-center rounded-xl bg-blue-50 text-blue-600 group-hover:bg-blue-600 group-hover:text-white transition-colors">
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d={s.icon} />
            </svg>
          </div>

          <h3 class="text-xl font-bold text-gray-900 mb-2">{s.title}</h3>
          <p class="text-sm font-medium text-blue-600 mb-4">{s.subtitle}</p>
          <p class="text-gray-600 leading-relaxed mb-8">{s.desc}</p>

          <ul class="space-y-3 border-t border-gray-100 pt-6">
            {s.features.map((f) => (
              <li class="flex items-start gap-3">
                <div class="mt-1.5 h-1.5 w-1.5 flex-shrink-0 rounded-full bg-blue-500"></div>
                <div>
                  <span class="text-sm font-semibold text-gray-900">{f.title}</span>
                  <span class="text-sm text-gray-500 block">{f.desc}</span>
                </div>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>
